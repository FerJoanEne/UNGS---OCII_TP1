#include "functions.h"

int main(){
    
    printMessage(WELCOME);

    int active = 1;
    while (active == 1)
    {
        if(active == 1){
            leerPregunta();
        }
    }
    
    return 0;

}